.dev.vars
node_modules/
worker-configuration.d.ts
.wrangler/
.admin-token
.helius-key

# Never commit live-trading values
.env.live

# Collaborator credential (research tier). Never committed.
.research-token
